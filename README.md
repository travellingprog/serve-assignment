# ServeRobotics Assignment, by Erick Mendez Cardenas

This repo contains the assignment that Erick was tasked with doing, by Serve Robotics.

The application can be run with the command:

```sh
npm start
```

The front-end page can be seen by going to http://localhost:4000 in the browser
